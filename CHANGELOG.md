# 변경 이력

이 스킨의 모든 버전 변경 내용입니다. 최신 버전이 맨 위에 있습니다.

> 스킨 편집에 올릴 때는 보통 **`skin.html` · `style.css` · `images/script.js`** 세 개만
> 갈아끼우면 됩니다. `index.xml` 은 **옵션이 추가·변경된 버전**에서만 다시 올리세요
> (다시 올리면 저장해 둔 옵션 값이 초기화될 수 있습니다).

| 버전 | 날짜 | 변경사항 |
|---|---|---|
| v3.9.4 | 2026-08-22 | **긴 글에서 개요(목차)가 잘리던 문제 해결.** 목차 높이가 화면의 42%(`max-height: 42vh`)로 고정돼 있어 항목이 많으면 아래쪽이 잘렸고, 스크롤바까지 숨겨 둬서 더 있다는 표시조차 없었습니다. 더 큰 원인은 script.js 가 지금 읽는 위치를 **하이라이트만 하고 목차를 따라 굴려 주지 않던 것** — 본문을 내리면 활성 항목이 잘린 영역으로 들어가 목차가 멈춘 것처럼 보였습니다. 이제 ① 개요+광고 묶음을 화면 높이(`calc(100vh - 20px)`)로 묶고 flex 로 바꿔 **목차가 광고 쓰고 남은 세로 공간을 전부 가져갑니다**(다 들어가면 스크롤 없이 통째로, 넘치면 목차 안에서만 스크롤). 광고는 `flex: none` 이라 절대 잘리지 않습니다 ② 활성 항목이 안 보이면 **목차 안에서만 굴려 따라갑니다**(본문은 움직이지 않음) ③ 넘칠 때만 6px 얇은 스크롤바가 보이고, 목차 끝에서 본문이 딸려 내려가지 않습니다(`overscroll-behavior: contain`). 실측(뷰포트 1375px · 항목 20개) 목차 578px → **707px** 로 20개 전부 표시. 사이드 광고를 160×600 대신 **250×250** 으로 바꾸면 목차가 350px 더 길어집니다 — 사이드바 안쪽 폭이 256px 이라 250×250 이 폭에도 딱 맞습니다. `index.xml` 은 버전 숫자만 바뀌었으니 다시 올리지 않아도 됩니다 |
| v3.9.3 | 2026-08-22 | **태그 그래프를 화면에 들어올 때 읽도록 바꾸고(지연 로드), 노드 상한을 220 → 550 으로 올렸습니다.** RSS는 글 본문까지 담고 있어 첫 화면을 그리는 동안 받을 이유가 없습니다. 그래프를 숨기는 좁은 화면에서는 아예 받지 않습니다. 노드 상한은 RSS 공개 개수를 50으로 열었을 때 글 48 + 태그 290 = 338 개가 처음부터 들어오는 것을 실측해 정했습니다(펼칠 자리 200 여유). 캐시 키를 `devlog-graph-v2` 로 올려, RSS 설정을 바꿔도 예전 그래프가 하루 동안 남아 있던 문제를 함께 없앴습니다. **그래프를 촘촘하게 만드는 건 스킨이 아니라 RSS 설정입니다** — 관리 > 블로그 에서 공개 개수를 올리고 부분공개로 바꾸면 노드가 4배가 됩니다(README 참고) |
| v3.9.2 | 2026-08-22 | **북마크 카드를 이미지가 꽉 채웁니다.** 예전에는 이미지 아래에 불투명한 텍스트 칸이 따로 붙어 그 경계가 선처럼 보였는데, 제목·부제목을 이미지 **위에** 얹고 글자가 놓이는 자리만 아래로 갈수록 짙어지는 반투명 그라데이션으로 덮었습니다. 텍스트 칸이 사라진 만큼 이미지 세로 비율을 16:9 → **4:3** 으로 키웠습니다(폭 180px 기준 이미지 100px → 136px). 가로가 긴 이미지는 좌우가 약 25% 더 잘리니, 글자가 들어간 이미지는 가운데로 오게 두는 편이 좋습니다. 이미지 없이 배경색만 쓰는 카드는 큰 제목이 그대로 남고, 그 아래 같은 제목이 한 번 더 찍히던 중복만 없앴습니다 |
| v3.9.1 | 2026-08-22 | 북마크 카드에 마우스를 올리면 **이미지가 살짝 확대**됩니다(1.08배, 0.35초). 카드 테두리 안에서만 커지도록 잘라내기 때문에 옆 카드를 밀거나 레이아웃이 흔들리지 않습니다. 이미지 없이 배경색만 쓰는 카드는 지금처럼 테두리·배경색만 바뀝니다. 동작 최소화(prefers-reduced-motion)를 켠 방문자에게는 애니메이션이 나가지 않습니다 |
| v3.9.0 | 2026-08-21 | **북마크 카드를 4장 → 8장으로 확대.** 한 줄에 네 장씩 두 줄로 놓입니다. 스킨 편집에 "북마크 5~8" 그룹이 추가되어 옵션이 53개 → 77개가 되었습니다. 카드가 8장이면 좁은 폭에서 세로로 길어지는 문제가 있어, 1000px 이하에서 2열로 떨어뜨리던 규칙을 없애 4열을 유지하도록 하고(900px 기준 북마크 영역 1154px → 347px), 모바일에서 카드가 5장 이상이면 조금 촘촘하게 그립니다 |
| v3.8.7 | 2026-08-21 | 글 위쪽 태그 줄 여백을 9px → **12px** 로 조정 — 9px 로는 위아래 줄과 여전히 붙어 보였습니다 |
| v3.8.6 | 2026-08-21 | **글 위쪽 태그 줄에 여백 추가** — 태그는 알약 배경이 글자보다 위아래로 넓고 두 줄까지 늘어나서, 다른 줄과 같은 3px 여백으로는 `date` · `read` 줄에 달라붙어 보였습니다. 이 줄만 위아래 9px 로 넓혔습니다(`.prop:has(> .post-tags)`). 위아래를 함께 늘려 라벨과 태그가 같이 내려가므로 정렬은 그대로입니다 |
| v3.8.5 | 2026-08-21 | **글 위쪽 프로퍼티 표 세로 정렬 정확히 맞춤** — 왼쪽 라벨(`category` · `date` · `tags` · `read`)에 `padding-top: 1px` 이 걸려 있어 모든 줄에서 라벨이 오른쪽 값보다 1px 씩 내려가 있었습니다. 태그 줄은 알약의 줄상자(20.4px)가 라벨(21.6px)과 달라 1.5px 까지 벌어졌습니다. 라벨 여백을 걷어내고 태그 알약의 `line-height` 를 1.8 로 맞춰 네 줄 모두 글자 윗선이 정확히 일치합니다 |
| v3.8.4 | 2026-08-21 | ① **본문 제목 크기 확대** — h1 35px · h2 30px · h3 25px (모바일은 26 · 23 · 19px)로 키워 문단 구분이 또렷해집니다 ② **글 아래 태그 줄 높이 어긋남 수정** — `.post-tags` 에 걸려 있던 위쪽 여백(24px) 때문에 왼쪽 `tags` 라벨과 태그가 한 줄에 맞지 않던 문제. 태그 사이 간격도 가로 6px · 세로 7px 로 정리하고 티스토리가 넣는 쉼표를 감췄습니다 |
| v3.8.3 | 2026-08-20 | ① **다크모드로 바꾸면 본문 일부가 사라지던 버그 수정** — 테마 전환 애니메이션(0.2초) 도중에 명암 보정이 실행돼 "바뀌기 전 배경"을 재고 글자색을 정반대로 칠했습니다. 이제 전환이 끝난 뒤에 다시 재고, 스킨 배경 위 글자는 `var(--text-normal)` 을 써서 테마를 따라가게 했습니다 ② **본문 가독성 가드 확대** — 복사해 붙여넣은 글의 투명도(`opacity`)·`-webkit-text-fill-color` 까지 되살리고, 인라인 스타일이 없는 요소도 검사합니다 ③ 구글 자동 광고가 데스크톱 폭(760px)으로 잡혀 좁은 화면에서 본문을 덮던 문제 방지 ④ 모바일 글 목록에서 대표 이미지가 행 높이를 꽉 채우도록 정렬(날짜 줄까지 걸침) ⑤ 고정글 콜아웃 — **별을 왼쪽 세로 가운데에 고정**하고 제목·카테고리를 같은 선에서 왼쪽 정렬(PC·모바일 동일) |
| v3.8.2 | 2026-08-20 | ① 모바일에서 홈 **인기 글 순위 번호(1~5) 숨김** — 제목이 두 줄로 접히며 겹쳐 보였습니다 ② **모바일 전용 사이드 광고 자리 추가**(`ad-side-m`, 달력 바로 아래, 250×250 권장) — 모바일에서는 세로로 긴 PC 사이드 광고를 숨깁니다 ③ **PC 좌측 사이드바 고정** — 글을 읽으며 내려가도 카테고리·방문 통계·스킨 정보가 계속 보입니다. 스크롤하면 프로필이 가로형으로 접혀 카테고리에 자리를 내줍니다 |
| v3.8.1 | 2026-08-20 | **v3.8.0 QA 후속 5건.** ① 현재 페이지 표시 재수정 — 티스토리가 `selected` 를 `<a>` 가 아니라 **안쪽 `<span>`** 에 붙인다는 것을 실제 블로그에서 확인하고, script.js 가 클래스를 `<a>` 로 끌어올리도록 변경(`···` 생략 표시도 누를 수 없게 처리) ② 개요와 광고 사이 구분선이 두 줄로 보이던 문제 — 광고 자리의 `border-top` 제거 ③ 홈·목록에서는 광고를 **맨 아래(최근 댓글 다음)** 로 이동, 글 본문에서만 개요 아래에 유지 ④ 다크모드 제목 형광펜을 **불투명 파스텔 + 검정 글자**로 변경(반투명이라 배경과 섞여 칙칙해 보이던 문제) ⑤ 미리보기에 광고 자리 표시 상자와 실제 티스토리 페이지네이션 출력을 반영 |
| v3.8.0 | 2026-08-20 | **QA 지적 5건 반영.** ① 모바일 대응 — 상단 바 안으로 사이드바 버튼을 옮기고, 우측 사이드바 내용을 본문 아래 카드로 재배치, 노치 영역(safe-area) 대응, 터치 영역 38px 확보 ② 본문 표 — 머리글 배경 · 줄무늬 · 또렷한 테두리로 구분감 부여, 에디터가 박아 둔 흰 배경 셀을 걷어내고 좁은 화면에서는 가로 스크롤 ③ 폭별 레이아웃 깨짐 — 반응형 규칙이 뒤쪽 섹션에 덮여 우측 사이드바가 안 숨겨지던 문제 수정(하단 빈 여백 · 그래프 납작해짐의 원인), 우측 사이드바 유지 구간을 1400px → 1180px 로 확대, 비어 있는 광고 자리가 여백을 차지하던 문제 수정 ④ 페이지네이션 — 티스토리가 넣는 `class="selected"` 에 스킨 class 가 덮여 현재 페이지 표시가 사라지던 문제 수정 ⑤ 글자 가독성 — 형광펜 위 글자색 자동 보정, 옅은 글자색(`--text-faint`)과 코드 주석색을 WCAG AA(4.5:1)에 맞게 조정 |
| v3.7.1 | 2026-08-20 | 배포용 / 개인용 두 벌 관리 구조 추가 — `build_personal.py` 로 개인 광고·분석 코드와 스킨 편집 옵션 값(고정글·북마크·about·저작권 등)을 자동 주입해, 새 버전으로 바꿔도 다시 넣을 필요가 없습니다 |
| v3.7.0 | 2026-08-19 | 광고 자리 3곳 추가(목록 상단 · 글 본문 아래 · 사이드바) — 코드를 넣지 않으면 자리 자체가 사라집니다. 사이드바 광고는 개요와 한 묶음으로 화면에 붙어 따라옵니다 |
| v3.6.2 | 2026-08-19 | 코드블록 서체를 IBM Plex 로 교체 — 영문·숫자·기호는 IBM Plex Mono(고정폭), 한글은 IBM Plex Sans KR |
| v3.6.1 | 2026-08-19 | 글을 읽을 때 우측 사이드바를 달력 · 그래프 · 개요만 남기도록 변경. 개요가 화면에 붙어 따라다니면서 아래 목록과 글자가 겹치던 문제 해결 |
| v3.6.0 | 2026-08-19 | 제목 형광펜을 글자 크기에 딱 맞추고 모서리를 각지게 변경. 좌우 사이드바가 본문과 함께 스크롤되도록 바꿔 사이드바 스크롤바 제거(개요는 sticky 로 따라옴). 좌측 블로그 메뉴 패널 삭제. 코드블록·인라인 코드 서체를 엘리스 디지털배움체로 교체(`--font-code`) |
| v3.5.2 | 2026-08-19 | 글 페이지에서 최근 글·인기 글·최근 댓글이 접히지 않던 문제 수정(v3.5.0에서 사이드바 순서를 손보다 접힘 CSS가 함께 지워졌음). 좌측 사이드바 방문 통계와 메뉴 사이에 스킨 제작 표기 추가 |
| v3.5.1 | 2026-08-19 | 그래프 노드를 카테고리·태그별 색으로 구분(10색 팔레트). 댓글 영역 네임카드 썸네일이 56px 안에서 작게 쏠려 보이던 문제 수정 |
| v3.5.0 | 2026-08-19 | **태그 그래프(그래프 뷰) 추가** — 우측 사이드바 달력 아래 고정. RSS로 글↔태그 관계를 읽어 canvas 로 그리고, 점을 누르면 이어진 글·태그가 펼쳐집니다. 드래그·확대·크게 보기 지원, 외부 라이브러리 없음. 우측 사이드바 순서를 달력 → 그래프 → 개요 → 최근 글 → 인기 글 → 최근 댓글 로 고정 |
| v3.4.1 | 2026-08-19 | 북마크 카드의 외부 링크 아이콘을 항상 표시 — 숨겨 두면 제목과 부제목의 가운데가 어긋나 보이던 문제 |
| v3.4.0 | 2026-08-19 | 본문 h1·h2·h3 에 파스텔 형광펜 효과(스크롤 시 그어짐), 글 아래 저작권 문구 옵션, 복사 시 출처 자동 첨부 / 우클릭·드래그 차단 옵션 추가. 사이드바 제목 높이 50px, 북마크 제목·부제목 가운데 정렬 |
| v3.3.0 | 2026-08-19 | 고정폭 서체 모노플렉스KR(SIL OFL) 도입 — 코드블록과 좌측 카테고리 트리에 적용, 좌측 사이드바 폭 288 → 320px 확대. 프로필 사진 150px |
| v3.2.0 | 2026-08-19 | SEO 보강 — h1 중복 제거, BreadcrumbList 구조화 데이터, `<time datetime>`, 이미지 alt, 검색 페이지 noindex, `<main>` 랜드마크와 본문 바로가기 추가. 티스토리가 이미 넣는 og·description·canonical·BlogPosting 은 중복되지 않게 그대로 둠. 홈에서 중복이던 WebSite 구조화 데이터 제거. 광고·분석 코드는 스킨 편집 옵션이 아니라 HTML의 지정된 자리에 직접 붙여넣는 방식으로 변경(티스토리가 옵션 값의 script 를 차단) |
| v3.1.0 | 2026-08-19 | 본문 서체를 자연 산스로 교체. 고정글은 **글 주소만** 넣으면 제목·카테고리를 자동으로 읽어오도록 변경, 북마크에 켜기/끄기 토글 추가(기본값 비움), 외부 연동에 **복사한 코드를 통째로 붙여넣어도** 인식하도록 개선, about 문구를 스킨 편집에서 직접 입력 |
| v3.0.0 | 2026-08-19 | 우측 하단 상태바(오늘·누적 카운트, 개인정보처리방침 링크) 제거 — 사이드바 방문 통계와 중복. 글 목록 행 높이·대표 이미지 확대(148×108), 인기 글 제목이 32자에서 잘리던 문제 해결(`lengthOfRecentEntry` 100). **배포용 정리** — 특정 블로그 전용 값을 전부 제거. 홈 카테고리 타일 기능 삭제(티스토리가 카테고리를 하나씩 도는 치환자를 주지 않아 어느 블로그에서나 쓰기 어려움 — 왼쪽 사이드바 트리로 대체), 기술 스택 태그와 애드센스·GTM·사이트 인증 코드를 스킨 편집 옵션으로 분리, 고정글 강조색을 강조(포인트) 색상으로 변경 |
| v2.8.2 | 2026-08-19 | 카테고리 타일이 하나도 안 나오던 문제 해결 — 실제 티스토리 트리에는 이름에 걸린 `<a>` 가 없고 `onclick` 으로만 이동한다는 것을 반영. 타일 주소를 onclick 에서 직접 읽어 하위 카테고리 경로까지 정확히 연결 |
| v2.8.1 | 2026-08-19 | 카테고리 타일 매칭 수정 — 하위 카테고리(Java·KeyMap 등)를 못 찾던 문제와 한글 이름(보물창고 등)이 엉뚱한 카테고리에 잡히던 문제 해결 |
| v2.8.0 | 2026-08-19 | 홈 카테고리 타일을 목록에서 고르는 방식으로 변경(직접 입력 → 선택), 고정글을 글 주소로 직접 지정(최대 3개), 홈 블록 사이 여백 확대, 프로필 설명 줄바꿈·들여쓰기 유지 + 가운데 정렬 + 3줄 확보, 최근 댓글이 비었을 때 안내 문구 표시, 애드센스 자동 광고가 프로필 위로 끼어드는 현상 방지 |
| v2.7.0 | 2026-08-19 | 홈 고정글을 스킨 편집에서 켜고 끄기 + 개수(1~3) 선택, 고정글 전용 강조 색상 적용. 리본의 RSS·테마 버튼을 상단으로 이동. 방문 통계의 일자별 그래프를 "어제 대비 오늘" 추이 막대로 교체 |
| v2.6.0 | 2026-08-19 | 글 목록 행 구조 정리 — 대표 이미지가 행 높이를 꽉 채우도록 변경, 카테고리 줄·댓글 수 표시 제거 |
| v2.5.0 | 2026-08-19 | 홈 카테고리 타일을 스킨 편집에서 설정하도록 변경 — 표시 개수 0~4개, 표시할 카테고리 직접 선택(하위 카테고리 포함), 비우면 자동 |
| v2.4.0 | 2026-08-19 | 홈 본문에 인기 글 목록 추가. 최근 글·인기 글의 표시 여부·순서·개수를 스킨 편집에서 설정. 홈의 "전체 글 000" 중복 헤더 제거 |
| v2.3.0 | 2026-08-19 | 사이드바에 인기 글 목록 추가. 최근 글 · 인기 글 · 최근 댓글의 표시 여부와 개수를 스킨 편집에서 설정 |
| v2.2.1 | 2026-08-19 | 북마크가 1~3장일 때 카드가 통째로 늘어나던 문제 수정 (4열 고정) |
| v2.2.0 | 2026-08-19 | 홈 북마크 4개를 스킨 편집 패널에서 설정하도록 변경(제목·부제목·링크·이미지·배경색), 디자인 색상 옵션 실제 적용 및 --accent-rgb 자동 동기화 |
| v2.1.0 | 2026-08-19 | 홈 stack 태그 링크 수정(상대경로), 글 상세 우측 사이드바 순서 변경(달력→개요→최근글→최근댓글) 및 최근 글·댓글 기본 접힘 |
| v2.0.4 | 2026-08-19 | 카테고리 타일이 `/null` 로 이동하던 문제 수정 (href 속성이 없는 구조까지 대응) |
| v2.0.3 | 2026-08-19 | 카테고리 타일 클릭 이동 수정 (href="#" + onclick 구조 대응), 프로필 여백 확대 |
| v2.0.2 | 2026-08-19 | 프로필을 원형 대표 이미지 세로형으로 확대(사이드바 288px), 글 목록에 대표 이미지 노출 |
| v2.0.1 | 2026-08-19 | 카테고리 타일 자동 생성(사이드바 트리 기반), 미설정 북마크 카드 자동 숨김 |
| v2.0.0 | 2026-08-19 | Obsidian 기반 전면 리디자인 — 리본·파일 탐색기·탭 바·프로퍼티·콜아웃·백링크·개요 도입, 홈 북마크/고정글 추가, Font Awesome·Chart.js 제거 |
| v1.2 | 2026-06-19 | (DevLog Dark) 사이드바 모듈 타이틀 주석 위치 수정 |
| v1.0 | 2026-05-19 | (DevLog Dark) 최초 배포 |
